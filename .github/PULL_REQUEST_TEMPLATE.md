## What changed and why

<!-- Describe the change and the motivation behind it. -->

## Checklist

- [ ] `docker build .` succeeds locally
- [ ] `entrypoint.sh` remains POSIX-compatible (`/bin/sh`)
- [ ] `health-proxy.js` has no new npm dependencies
- [ ] Semver bump needed: yes / no — if yes, will tag after merge
